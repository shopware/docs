name: reviewdog
on: [pull_request]
jobs:
  linter_name:
    name: Check grammar and language
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: reviewdog/action-languagetool@v1
        with:
          reporter: github-pr-review
          level: info
